Go is a compiler language, transforms human-readable source code into machine-executable binaries.

Go's static typing is useful for backend work, types are checked in compile time.
- NO surprise type bugs in production
- IDEs can catch errors accurately
- function signatures tell you exactly what goes in and comes out
- knowing types at compile time lets the compiler generate more ==efficient== machine code

