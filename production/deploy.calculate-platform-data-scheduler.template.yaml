kind: Deployment
apiVersion: apps/v1
metadata:
  name: calculate-platform-data-scheduler-deployment
  namespace: bms
spec:
  replicas: 1
  selector:
    matchLabels:
      app: calculate-platform-data-scheduler-deployment
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: calculate-platform-data-scheduler-deployment
    spec:
      containers:
        - name: calculate-platform-data-scheduler
          image: 741328073657.dkr.ecr.ap-northeast-2.amazonaws.com/bms:{TAG}
          imagePullPolicy: Always
          command: [ "pm2-runtime" ]
          args: [ "start", "./pm2-processes/production.json", "--only", "calculate-platform-data-scheduler" ]
          volumeMounts:
            - name: log
              mountPath: /var/log/bms
          resources:
            limits:
              cpu: "1"
              memory: "1Gi"
            requests:
              cpu: "0.1"
              memory: "100Mi"

      volumes:
        - name: log
          hostPath:
            path: /var/log/bms/production
            type: DirectoryOrCreate

      nodeSelector:
        app: bms
        env: production
