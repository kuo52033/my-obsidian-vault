kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: bms-platform-ingress
  namespace: bms
  annotations:
    kubernetes.io/ingress.class: "bms"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "3600"
    nginx.ingress.kubernetes.io/upstream-hash-by: "$x_forwarded_for"
spec:
  ingressClassName: bms
  rules:
    - host: bms-p.rcppay.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bms-platform-service
                port:
                  number: 80

---

kind: Service
apiVersion: v1
metadata:
  name: bms-platform-service
  namespace: bms
spec:
  ports:
    - port: 80
      targetPort: 3001
  selector:
    app: bms-platform-deployment

---

kind: Deployment
apiVersion: apps/v1
metadata:
  name: bms-platform-deployment
  namespace: bms
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bms-platform-deployment
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: bms-platform-deployment
    spec:
      containers:
        - name: platform
          image: 741328073657.dkr.ecr.ap-northeast-2.amazonaws.com/bms:{TAG}
          imagePullPolicy: Always
          ports:
            - containerPort: 3001
          command: [ "pm2-runtime" ]
          args: [ "start", "./pm2-processes/production.json", "--only", "platform" ]
          livenessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 30
            timeoutSeconds: 15
            successThreshold: 1
            periodSeconds: 15
          volumeMounts:
            - name: log
              mountPath: /var/log/bms
          resources:
            limits:
              cpu: "1.5"
              memory: "1.5Gi"
            requests:
              cpu: "0.1"
              memory: "200Mi"

      volumes:
        - name: log
          hostPath:
            path: /var/log/bms/production
            type: DirectoryOrCreate

      nodeSelector:
        app: bms
        env: production
